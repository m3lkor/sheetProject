<%= form_for(@gdr_module) do |f| %>
  <% if @gdr_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gdr_module.errors.count, "error") %> prohibited this gdr_module from being saved:</h2>

      <ul>
      <% @gdr_module.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :module_name %><br />
    <%= f.text_field :module_name %>
  </div>
    <div class="field">
    <%= f.label :game_id %><br />
    <%= f.collection_select :game_id, Game.all,:id,:game_name, { :prompt => 'Select a Game' } %>  
  </div>
  <div class="field">
    <%= f.label :module_type %><br />
    <%= f.number_field :module_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
